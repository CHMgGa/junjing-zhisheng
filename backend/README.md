# Backend Service and MySQL Data Management

## Configuration

Copy `src/main/resources/application.yml.example` to `application.yml`, then set the local MySQL, mail, and Huawei Cloud IoT values through environment variables. Do not commit real configuration files, passwords, cloud keys, or accounts.

## Run

```bash
mvn spring-boot:run
```
